import React from 'react';

export const AnimatedBackground: React.FC = () => {
  // El usuario solicitó eliminar los glows del fondo por completo.
  // Al retornar null, se ve el color de fondo base (negro #000000 definido en index.html).
  return null;
};